# Model checkpoints

Large model checkpoint files are intentionally not included in this GitHub repository.

The trained checkpoints can be regenerated from the notebooks and scripts. If final archival is required, model checkpoints should be uploaded separately to Zenodo rather than GitHub.
